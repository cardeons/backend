# frozen_string_literal: true

require 'rails_helper'

RSpec.describe Monstercard, type: :model do
    # subject do
    #     described_class.new(
    #       id: 5,
    #       position: 'inventory',
    #     )
    #   end

    #   it 'finds the right card' do
    #     subject = Ingamedeck.find_by(id: 5)
    #     puts subject
    #     expect(subject).to be_valid
    #   end
end
