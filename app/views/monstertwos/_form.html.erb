<%= form_with(model: monstertwo) do |form| %>
  <% if monstertwo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(monstertwo.errors.count, "error") %> prohibited this monstertwo from being saved:</h2>

      <ul>
        <% monstertwo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ingamedeck_id %>
    <%= form.text_field :ingamedeck_id %>
  </div>

  <div class="field">
    <%= form.label :player_id %>
    <%= form.text_field :player_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
