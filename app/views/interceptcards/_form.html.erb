<%= form_with(model: interceptcard) do |form| %>
  <% if interceptcard.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(interceptcard.errors.count, "error") %> prohibited this interceptcard from being saved:</h2>

      <ul>
        <% interceptcard.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :gameboard_id %>
    <%= form.text_field :gameboard_id %>
  </div>

  <div class="field">
    <%= form.label :ingamedeck_id %>
    <%= form.text_field :ingamedeck_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
