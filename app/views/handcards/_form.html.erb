<%= form_with(model: handcard) do |form| %>
  <% if handcard.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(handcard.errors.count, "error") %> prohibited this handcard from being saved:</h2>

      <ul>
        <% handcard.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ingamedeck_id %>
    <%= form.text_field :ingamedeck_id %>
  </div>

  <div class="field">
    <%= form.label :player_id %>
    <%= form.text_field :player_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
